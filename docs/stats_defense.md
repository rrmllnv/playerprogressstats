# 🛡️ Вкладка "Защита" (Defense)

---

## Статистики для вкладки "Защита"

### 🏃 Уклонения (Dodges):

| Статистика | Локализация | Описание | Флаг |
|-----------|-------------|----------|------|
| `max_dodges_in_a_row` | Своя: `stats_max_dodges` | Максимальное количество уклонений подряд | `backend` |
| `total_sprint_dodges` | Своя: `stats_sprint_dodges` | Общее количество уклонений спринтом | `backend` |
| `total_slide_dodges` | Своя: `stats_slide_dodges` | Общее количество уклонений скольжением | `backend` |

### 🛡️ Блокирование (Blocking):

| Статистика | Локализация | Описание | Флаг |
|-----------|-------------|----------|------|
| `max_damage_blocked_last_20_sec` | Своя: `stats_max_damage_blocked` | Максимальный заблокированный урон за 20 секунд | `backend` |

---

## 💻 Пример кода для вкладки:

```lua
-- tab_defense.lua

local layout = {}

-- Уклонения
table.insert(layout, {
    widget_type = "stat_line",
    text = localize("stats_max_dodges"),
    value = format_number(safe_read_stat("max_dodges_in_a_row"))
})

table.insert(layout, {
    widget_type = "stat_line",
    text = localize("stats_sprint_dodges"),
    value = format_number(safe_read_stat("total_sprint_dodges"))
})

table.insert(layout, {
    widget_type = "stat_line",
    text = localize("stats_slide_dodges"),
    value = format_number(safe_read_stat("total_slide_dodges"))
})

-- Блокирование
table.insert(layout, {
    widget_type = "stat_line",
    text = localize("stats_max_damage_blocked"),
    value = format_number(safe_read_stat("max_damage_blocked_last_20_sec"))
})
```

---

## 📋 Текущая реализация:

Файл: `PlayerProgressStats/scripts/mods/PlayerProgressStats/views/tabs/tab_defense.lua`

Отображает:
1. Максимум уклонений подряд
2. Уклонений спринтом
3. Уклонений слайдом
4. Максимум заблокированного урона за 20 сек

---

## 🔧 Дополнительные статистики защиты (не реализовано):

### Специфические уклонения:

| Статистика | Описание | Флаг |
|-----------|----------|------|
| `trapper_net_dodged` | Уклонения от сетей траппера | `backend` |
| `sniper_dodged` | Уклонения от выстрелов снайпера | `backend` |
| `shotgunner_spread_dodged` | Уклонения от залпов шоктрооперов | `backend` |
| `mutant_charge_dodged` | Уклонения от заряда мутанта | `backend` |
| `mauler_attack_dodged` | Уклонения от атак маулера | `backend` |
| `crusher_overhead_smash_dodged` | Уклонения от ударов крушера | `backend` |
| `cultist_gunner_shot_dodged` | Уклонения от выстрелов ганнера | `backend` |
| `ogryn_gunner_shot_dodged` | Уклонения от выстрелов огрина-ганнера | `backend` |

### Блокирование (расширенное):

| Статистика | Описание | Флаг |
|-----------|----------|------|
| `team_blocked_damage` | Заблокированный урон командой (за всё время) | `backend`, `team` |
| `session_my_blocked_damage` | Заблокированный урон в сессии | `no_sync` |
| `session_team_blocked_damage` | Заблокированный урон командой в сессии | `no_sync` |
| `damage_blocked_last_20_sec` | Заблокированный урон за последние 20 сек (динамический) | `no_sync` |
| `hook_blocked_damage_from_unique_enemy` | Hook для уникального врага | `no_sync` |

---

## 🎮 Сессионные статистики защиты:

⚠️ **Внимание:** Эти статистики имеют флаг `StatFlags.no_sync` - доступны только во время миссии!

| Статистика | Описание |
|-----------|----------|
| `dodges_in_a_row` | Уклонения подряд (сбрасывается при попадании) |
| `session_my_blocked_damage` | Мой заблокированный урон в текущей сессии |
| `session_team_blocked_damage` | Заблокированный урон командой в сессии |

---

## 📝 Локализация в файле мода:

Файл: `PlayerProgressStats/scripts/mods/PlayerProgressStats/PlayerProgressStats_localization.lua`

```lua
stats_max_dodges = {
    en = "Max Dodges in a Row",
    ru = "Максимум уклонений подряд",
},
stats_sprint_dodges = {
    en = "Sprint Dodges",
    ru = "Уклонений спринтом",
},
stats_slide_dodges = {
    en = "Slide Dodges",
    ru = "Уклонений слайдом",
},
stats_max_damage_blocked = {
    en = "Max Damage Blocked (20s)",
    ru = "Максимум блока урона (20 сек)",
},
```

---

## 🔍 Источники данных:

- `stat_definitions.lua`:
  - Строки 986-1018: `dodges_in_a_row`, `max_dodges_in_a_row`
  - Строки 1019-1048: `total_sprint_dodges`, `total_slide_dodges`
  - Строки 1049-1128: блокирование урона
  - Строки 2352-2528: специфические уклонения

### Примечания по уклонениям:

**Подряд (in_a_row):**
- Счётчик сбрасывается при получении урона
- Максимальное значение сохраняется в `max_dodges_in_a_row`

**Типы уклонений:**
- Sprint dodge - уклонение из спринта
- Slide dodge - уклонение из скольжения
- Обычное уклонение - базовое

### Примечания по блокированию:

**Окно времени:**
- `max_damage_blocked_last_20_sec` - скользящее окно 20 секунд
- Динамически обновляется во время миссии
- Сохраняется максимальное значение за всё время

**Командная статистика:**
- `team_blocked_damage` суммирует урон всей команды
- Флаг `StatFlags.team`

