# 🔍 Интерактивная проверка локализации

---

## 📖 Описание

Вкладка **"Localization Check"** (доступна только при `DEBUG = true`) предоставляет интерактивный интерфейс для проверки ключей локализации игры.

### Отличие от вкладки "Localization":

| Вкладка | Назначение | Как работает |
|---------|-----------|--------------|
| **Localization** | Статичный список тестовых ключей | Все ключи отображаются сразу |
| **Localization Check** | Интерактивный выбор категорий | Выбираете категорию → видите её ключи |

---

## 🎮 Как использовать

### Шаг 1: Включите DEBUG режим

В файле `player_progress_stats_view.lua`:
```lua
local DEBUG = true  -- Установите в true
```

### Шаг 2: Откройте мод в игре

- Зайдите в хаб
- Откройте мод через горячую клавишу или DMF Options
- Перейдите на вкладку **"✅ LOC CHECK"** (последняя вкладка)

### Шаг 3: Выберите категорию

Вы увидите список категорий:
- 🐛 Breeds - Bosses
- ⚔️ Breeds - Elites
- ⚡ Breeds - Specials
- 🏆 Achievements
- 🔫 Weapons
- 🎮 UI Elements
- 📊 Stats & Numbers
- 🌍 Zones

### Шаг 4: Просмотрите результаты

После клика на категорию:
- **Слева вверху**: ключ локализации (например: `loc_breed_display_name_chaos_daemonhost`)
- **Справа внизу**: перевод (например: `Daemonhost`) или `[NOT FOUND]`

### Шаг 5: Вернитесь к списку

Нажмите **"◄ Back to Categories"** вверху экрана

---

## 🔧 Добавление новых категорий

### Структура файла

Файл: `views/tabs/tab_localization_check.lua`

```lua
local localization_categories = {
    {
        name = "🐛 Breeds - Bosses",  -- Название категории
        keys = {                       -- Список ключей
            "loc_breed_display_name_chaos_daemonhost",
            "loc_breed_display_name_chaos_beast_of_nurgle",
            -- ... другие ключи
        }
    },
    -- ... другие категории
}
```

### Добавление новой категории

1. Откройте `tab_localization_check.lua`
2. Найдите массив `localization_categories`
3. Добавьте новую категорию:

```lua
{
    name = "🎨 Ваша категория",
    keys = {
        "loc_your_key_1",
        "loc_your_key_2",
        "loc_your_key_3",
    }
},
```

4. Сохраните и перезагрузите мод

---

## 💡 Примеры категорий

### Категория для класса (Veteran)

```lua
{
    name = "🎯 Veteran - Abilities",
    keys = {
        "loc_ability_veteran_shout",
        "loc_ability_veteran_volley_fire",
        "loc_talent_veteran_improved_tag",
        "loc_trait_veteran_rending_on_critical_hit",
    }
},
```

### Категория для оружия конкретного типа

```lua
{
    name = "🔫 Lasguns",
    keys = {
        "loc_weapon_pattern_infantry_lasgun_m1",
        "loc_weapon_pattern_infantry_lasgun_m2",
        "loc_weapon_pattern_infantry_lasgun_m3",
        "loc_weapon_keyword_high_accuracy",
    }
},
```

### Категория для миссий

```lua
{
    name = "🗺️ Mission Names",
    keys = {
        "loc_mission_name_lm_scavenge",
        "loc_mission_name_cm_raid",
        "loc_mission_name_dm_stockpile",
        "loc_mission_name_km_enforcer",
    }
},
```

---

## 🎨 Кастомизация внешнего вида

### Blueprint'ы для интерактивных элементов

Определены в `player_progress_stats_view.lua`:

1. **category_button_blueprint** - Кнопка категории
   - Цвет фона: `Color.terminal_background(128, true)`
   - При наведении: alpha увеличивается до 200
   - Рамка: `content/ui/materials/frames/frame_tile_2px`

2. **back_button_blueprint** - Кнопка "Назад"
   - Цвет фона: `Color.terminal_corner(128, true)`
   - Текст по центру
   - Специальная рамка

3. **debug_line_blueprint** - Строка с ключом и переводом
   - Высота: 70px (увеличена для длинных текстов)
   - Ключ: слева вверху, меньший шрифт
   - Перевод: справа внизу, крупный шрифт

### Изменение высоты строк

В `player_progress_stats_view.lua`:

```lua
local category_button_blueprint = {
    size = {grid_size[1] - 20, 50},  -- Измените 50 на другое значение
    -- ...
}
```

---

## 🐛 Отладка

### Проблема: Кнопки не кликаются

**Решение:**
- Проверьте что `TabLocalizationCheck` корректно импортирован
- Убедитесь что `DEBUG = true`
- Проверьте console на ошибки Lua

### Проблема: Не обновляется grid после клика

**Решение:**
- Проверьте что `parent_view` передаётся в element
- Убедитесь что `_update_grid_content` вызывается в blueprint.update

### Проблема: Все ключи показывают [NOT FOUND]

**Решение:**
- Проверьте правильность написания ключей
- Ключи должны начинаться с `loc_`
- Используйте официальные ключи из исходников игры

---

## 📊 Статистика использования

### В конце каждой категории отображается:

```
Total keys in category: 7
```

Показывает количество ключей в выбранной категории.

---

## 🔍 Источники ключей локализации

### Где найти официальные ключи:

| Источник | Где искать | Примеры |
|----------|-----------|---------|
| **Породы врагов** | `breeds/{faction}/{breed}_breed.lua` | `display_name = "loc_breed_display_name_*"` |
| **Достижения** | `achievement_definitions.lua` | `display_name`, `description` |
| **Оружие** | `weapon_templates/` | `display_name = "loc_weapon_*"` |
| **UI** | `ui/` директория | `loc_settings_*`, `loc_menu_*` |
| **Таланты** | `player_character_talents.lua` | `display_name = "loc_talent_*"` |

### Поиск ключей в исходниках:

```lua
-- Grep для поиска ключей
grep -r "display_name.*loc_" Darktide-Source-Code/
```

---

## 🚀 Расширенные возможности

### Добавление счётчика найденных/не найденных

Можно модифицировать `tab_localization_check.lua`:

```lua
local found = 0
local not_found = 0

for i, key in ipairs(category.keys) do
    local translated_text = localize(key)
    if translated_text ~= key then
        found = found + 1
        -- ...
    else
        not_found = not_found + 1
        -- ...
    end
end

table.insert(layout, {
    widget_type = "stat_line",
    text = "Found / Not Found:",
    value = found .. " / " .. not_found
})
```

### Фильтрация по статусу

Можно добавить кнопки фильтрации:
- Показать только найденные
- Показать только не найденные
- Показать все

---

## 📝 Примечания

1. **Производительность**: Большое количество ключей может замедлить отрисовку
2. **Локализация**: Переводы зависят от языка игры
3. **Кеширование**: Ключи не кешируются, проверка происходит в реальном времени

---

**Версия:** 1.0  
**Дата:** 2025-12-01  
**Автор:** PlayerProgressStats Mod

