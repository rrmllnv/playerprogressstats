# 📊 Darktide Statistics Reference / Справочник статистик Darktide

---

## 📚 Документация по вкладкам:

Справочник разбит на отдельные файлы по вкладкам мода для удобства:

### 🗂️ Навигация по документации:

| Вкладка | Файл документации | Описание |
|---------|------------------|----------|
| **📊 Общее** | [`docs/stats_general.md`](docs/stats_general.md) | Общие убийства, убийства по фракциям, специальные убийства, компаньон |
| **⚔️ Враги** | [`docs/stats_enemies.md`](docs/stats_enemies.md) | Боссы, элиты, специалисты - подробная разбивка по типам врагов |
| **🗺️ Миссии** | [`docs/stats_missions.md`](docs/stats_missions.md) | Миссии, Auric, Havoc, ранги, безупречные миссии |
| **🏆 Рекорды** | [`docs/stats_records.md`](docs/stats_records.md) | Максимальные достижения, хедшоты, помощь, выносливость |
| **🛡️ Защита** | [`docs/stats_defense.md`](docs/stats_defense.md) | Уклонения, блокирование, специфические уклонения |

---

## 🚀 Быстрый старт

### Как использовать локализацию:

```lua
-- В функции localize() сначала проверяется официальная локализация игры
local text = localize("loc_breed_display_name_chaos_daemonhost")
-- Вернёт переведенное название на языке игрока

-- Пример использования
table.insert(layout, {
    widget_type = "stat_line",
    text = localize("loc_achievement_enemies_killed_by_barrels_name"),  -- Официальная
    value = format_number(safe_read_stat("enemies_killed_with_barrels"))
})

-- Или использовать свою локализацию
table.insert(layout, {
    widget_type = "stat_line",
    text = localize("stats_total_kills"),  -- Из PlayerProgressStats_localization.lua
    value = format_number(safe_read_stat("total_kills"))
})
```

### Как читать статистики:

```lua
-- Безопасное чтение статистики
local function safe_read_stat(stat_name)
    if not Managers or not Managers.stats then
        return 0
    end
    
    local success, value = pcall(function()
        return Managers.stats:read_user_stat(1, stat_name)
    end)
    
    return success and value or 0
end

-- Использование
local total_kills = safe_read_stat("total_kills")
```

---

## 🔍 Отладка локализации

### DEBUG режим:

В файле `player_progress_stats_view.lua` установите:
```lua
local DEBUG = true
```

Это добавит вкладку **"🔍 LOCALIZATION"** где можно тестировать ключи локализации.

### Как тестировать:

1. Откройте `views/tabs/tab_localization_debug.lua`
2. Добавьте ключи в массив `test_localization_keys`:
   ```lua
   "loc_your_test_key_here",
   ```
3. Откройте мод в игре → Вкладка "Локализация"
4. Увидите:
   - **Слева вверху**: ключ локализации (серый)
   - **Справа внизу**: перевод (жёлтый) или `[NOT FOUND]`

---

## 📁 Структура проекта

```
PlayerProgressStats/
  ├── docs/
  │   ├── stats_general.md     (Документация вкладки "Общее")
  │   ├── stats_enemies.md     (Документация вкладки "Враги")
  │   ├── stats_missions.md    (Документация вкладки "Миссии")
  │   ├── stats_records.md     (Документация вкладки "Рекорды")
  │   └── stats_defense.md     (Документация вкладки "Защита")
  │
  ├── scripts/mods/PlayerProgressStats/
  │   ├── views/
  │   │   ├── tabs/
  │   │   │   ├── tab_general.lua           (Код вкладки "Общее")
  │   │   │   ├── tab_enemies.lua           (Код вкладки "Враги")
  │   │   │   ├── tab_missions.lua          (Код вкладки "Миссии")
  │   │   │   ├── tab_records.lua           (Код вкладки "Рекорды")
  │   │   │   ├── tab_defense.lua           (Код вкладки "Защита")
  │   │   │   └── tab_localization_debug.lua (DEBUG вкладка)
  │   │   └── player_progress_stats_view.lua
  │   ├── PlayerProgressStats.lua
  │   └── PlayerProgressStats_localization.lua
  │
  └── STATS_REFERENCE.md (этот файл - навигация)
```

---

## 🎯 Типы статистик

### ✅ Глобальные статистики (StatFlags.backend)
Сохраняются на сервере, доступны всегда:
- `total_kills`, `total_*_killed`
- `missions`, `auric_missions`, `havoc_missions`
- `max_*` (максимальные значения)
- `total_sprint_dodges`, `total_slide_dodges`

### ❌ Сессионные статистики (StatFlags.no_sync)
Доступны только во время миссии, сбрасываются:
- `shots_fired`, `shots_missed`
- `session_*`
- `dodges_in_a_row` (текущая серия)
- `kills_last_60_sec` (динамическое окно)

---

## 📖 Дополнительные ресурсы

### Исходные файлы игры:

| Файл | Содержание |
|------|-----------|
| `scripts/managers/stats/stat_definitions.lua` | Все определения статистик (7456 строк) |
| `scripts/managers/achievements/achievement_definitions.lua` | Достижения и их локализация (4239 строк) |
| `scripts/settings/breed/breeds.lua` | Список всех пород врагов |
| `scripts/settings/breed/breeds/{faction}/{breed}_breed.lua` | Детальные параметры каждой породы |

### Система генерации статистик:

Многие статистики генерируются динамически:

```lua
-- stat_definitions.lua, строки 608-625
for i = 1, #breed_names do
    local breed_name = breed_names[i]
    local stat_id = string.format("total_%s_killed", breed_name)
    -- Создаётся total_chaos_daemonhost_killed, total_chaos_spawn_killed и т.д.
    
    StatDefinitions[stat_id] = {
        stat_name = Breeds[breed_name].display_name,
        -- display_name = "loc_breed_display_name_chaos_daemonhost"
    }
end
```

---

## 🛠️ Инструменты разработки

### Отладочная вкладка локализации:

Включите `DEBUG = true` в `player_progress_stats_view.lua` для:
- Тестирования ключей локализации
- Проверки официальных переводов игры
- Быстрого просмотра как выглядят тексты

### Структура модулей вкладок:

Каждый файл вкладки (`tab_*.lua`) экспортирует функцию:

```lua
local TabName = {}

TabName.create_layout = function(safe_read_stat, localize, format_number)
    local layout = {}
    
    -- Добавление статистик
    table.insert(layout, {
        widget_type = "stat_line",
        text = localize("key"),
        value = format_number(safe_read_stat("stat_name"))
    })
    
    return layout
end

return TabName
```

---

## 🌐 Локализация

### Приоритет локализации:

1. **Официальная локализация игры** (`loc_*` ключи)
   - Автоматически переводится на все языки
   - Используйте для породов врагов, достижений, UI элементов

2. **Локализация мода** (ключи без `loc_`)
   - Определяется в `PlayerProgressStats_localization.lua`
   - Нужно добавлять вручную для всех языков

### Функция localize():

```lua
local function localize(key)
    -- Если ключ начинается с "loc_" - пробуем официальную локализацию
    if key:match("^loc_") then
        local success, result = pcall(function()
            return Localize(key)  -- Глобальная функция игры
        end)
        
        if success and result and result ~= "" and result ~= key then
            return result
        end
    end
    
    -- Fallback на локализацию мода
    local success, result = pcall(function()
        return mod:localize(key)
    end)

    if success and result and result ~= "" then
        return result
    end

    return key  -- Если ничего не нашли - возвращаем сам ключ
end
```

---

## 📌 Полезные ссылки

- [Список всех статистик в stat_definitions.lua](file:///D:/OSPanel/domains/warhammer/Darktide-Source-Code/scripts/managers/stats/stat_definitions.lua)
- [Список достижений](file:///D:/OSPanel/domains/warhammer/Darktide-Source-Code/scripts/managers/achievements/achievement_definitions.lua)
- [Список пород врагов](file:///D:/OSPanel/domains/warhammer/Darktide-Source-Code/scripts/settings/breed/breeds.lua)

---

## 🆘 Частые вопросы

**Q: Почему статистика показывает 0?**
- Проверьте флаг: если `StatFlags.no_sync` - это сессионная статистика
- Сессионные статистики доступны только во время миссии
- Используйте только статистики с `StatFlags.backend`

**Q: Как найти локализацию для статистики?**
- Породы врагов: `loc_breed_display_name_{breed_name}`
- Достижения: ищите в `achievement_definitions.lua` по `stat_name`
- UI элементы: ищите по префиксу `loc_settings_*`, `loc_weapon_*`

**Q: Как добавить новую статистику?**
1. Откройте соответствующий `docs/stats_*.md`
2. Найдите нужную статистику и локализацию
3. Откройте соответствующий `tabs/tab_*.lua`
4. Добавьте новую строку с `table.insert(layout, ...)`
5. Перезагрузите мод

---

**Версия документации:** 1.0  
**Последнее обновление:** 2025-12-01
